# GroovyTunes
Group project for object oriented analysis and  design

server:
  to compile:
    run recompile.bat
  to run:
    run run_server.bat
	
client:
  to run:
    go to html folder
    run the server
    once server is running click on gui.html
